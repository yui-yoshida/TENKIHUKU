<%= form_with(model: @picture, local: true) do |form| %>
  <div class="TENKIHUKU_image">
    <%= form.label :image %>
    <%= imagae_tag(@picture.image.url) if @picture.image.url %>
    <%= form.file_field :image %>
  </div>
  <div class="TENKIHUKU_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
